build:
  docker:
    web: Dockerfile

run:
    web: python3 src/manage.py migrate && \
         python3 src/manage.py collectstatic --noinput && \
         cd src && \
         gunicorn gallery.wsgi:application --bind 0.0.0.0:$PORT
